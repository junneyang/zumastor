#!/bin/bash

if [[ $# -ne 1 ]] || [[ $1 != "install" ]]; then
	echo "error: upgrading not supported, remove old package first"
	exit 1
fi

if [[ -e /var/lib/zumastor ]] || [[ -e /etc/zumastor ]]; then
	echo "error: old files exist in /var/lib/zumastor or /etc/zumastor"
	exit 2
fi
